1 类型名称对齐
2 模块化
3 类型按功能 / 微服务... 拆分放好
4 区分类和 api declaration
5 每个 api 单独放在一个文件
6 输入输出和类型系统分开
7 前端 xxxApiMessage(input) -> Promise<output> 访问 /api/xxxApiMessage 而不是用 /api/auth/log-in；后端必须导出 message registry，前端必须基于该 registry 生成或校验 message 名称和 input/output 类型，做到前后端编译通过就基本没有问题
